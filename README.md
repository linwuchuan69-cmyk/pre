# Shorts Daily Digest

YouTubeショートで人気の動画を、日次でまとめて読めるシンプルなWebサイトです。

## できること
- 日付ごとの「人気ショートまとめ」を表示
- ジャンルで絞り込み
- 「今日のまとめを更新」で当日データを再生成
- ブラウザの `localStorage` に日次履歴を保存

## 起動方法
静的サイトなので任意のHTTPサーバーで起動できます。例:

```bash
python3 -m http.server 4173
```

その後、ブラウザで `http://localhost:4173` を開いてください。
